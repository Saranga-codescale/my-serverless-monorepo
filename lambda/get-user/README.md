# get-user

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build get-user` to build the library.

## Running unit tests

Run `nx test get-user` to execute the unit tests via [Jest](https://jestjs.io).
